# Screenshots

Drop your images here with these exact names so the main README picks them up:

| File                  | Suggested shot                                  |
| --------------------- | ----------------------------------------------- |
| `shop-menu.png`       | Shop side — the live menu (photos + categories) |
| `admin-dashboard.png` | Admin — live orders dashboard                   |
| `khata.png`           | Khata ledger (balance + transactions)           |
| `order-tracker.png`   | Shop — live order status tracker                |

Tips: use your real app, take them on a phone or a narrow browser window
(mobile view looks best for the shop side), PNG format.
